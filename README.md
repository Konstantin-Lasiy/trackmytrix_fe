# React + HttpOnly Cookie Auth + React-router-dom

This template provides a minimal setup to get React working in Vite with login persist, registration using httponly cookies.

- Axios 
- react-router-dom
